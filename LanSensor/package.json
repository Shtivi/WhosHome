{
  "name": "lan-sensor",
  "version": "1.0.0",
  "description": "Monitors connections to the LAN by scanning the entries in the arp table.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Shtivi",
  "license": "ISC",
  "dependencies": {
    "arp-monitor": "^0.1.1",
    "ws": "^4.1.0"
  }
}
